<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.IULMS" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/login_accent</item>
        <item name="statusBarBackground">@color/login_accent</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.IULMS" parent="Base.Theme.IULMS" />
</resources>